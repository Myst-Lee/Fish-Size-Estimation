# Model Artifacts

Place the final model files from the notebook here before uploading to GitHub:

```text
final_random_forest_model.joblib
final_scaler.joblib
final_species_columns.json
final_feature_columns.json
```

These should come from the stronger notebook folder:

```text
/content/drive/MyDrive/Colab Notebooks/final_fish_model/
```

Do not use `final_fish_model2` for the main app unless you intentionally want the weaker species + ratio model.
